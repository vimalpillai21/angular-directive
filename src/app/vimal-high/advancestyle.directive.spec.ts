import { AdvancestyleDirective } from './advancestyle.directive';

describe('AdvancestyleDirective', () => {
  it('should create an instance', () => {
    const directive = new AdvancestyleDirective();
    expect(directive).toBeTruthy();
  });
});
